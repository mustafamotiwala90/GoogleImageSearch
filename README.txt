a small app which does the following:

1. The initial screen is a textbox with a search button

2. User inputs a query string and clicks Search on the virtual keyboard or the button

3. Performs a query to Google API
https://developers.google.com/web-search/docs/reference to retrieve IMAGES
matching the query. 

4. Displays the images in a table view, in each cell as a thumbnail